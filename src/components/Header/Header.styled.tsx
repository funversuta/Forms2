import styled from 'styled-components';

export const Head = styled.form `
   display: flex; 
   text-align: center;
   flex-direction: column;
   max-width: 400px;
   margin: 20px auto;
   padding-top: 200px;
   font-size: 40px;
   `;
